# Japanese, through mozc. `ibus` is in its IUSE with a `+`, so the flag has to
# turn it off or the desktop gets a second input framework it never uses.
packages = ["app-i18n/mozc"]
package_use = ["app-i18n/mozc fcitx5 -ibus"]
input_method = "mozc"
